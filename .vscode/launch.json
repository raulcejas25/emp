{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run Go API (dev)",
            "type": "go",
            "request": "launch",
            "mode": "auto",
            "program": "${workspaceFolder}",
            "env": {
                "PORT": "8080",
                "ENV": "local"
            },
            "args": []
        }
    ]
}